{
  "name": "my-site",
  "private": true,
  "description": "devDependencies here are for editor type-checking only — nothing in this file participates in the build or runtime. The site ships from vendor/ via <script type=\"importmap\">, and the browser never reads this file.",
  "scripts": {
    "test": "bun run spa-server.ts",
    "vendor": "bun run scripts/vendor-fetch.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "hono": "^4.12.30"
  }
}
